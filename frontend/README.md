# NASRadio app

Flutter client for the NASRadio backend: phone, Android TV layout, Windows
desktop. See the repository README for setup and the first-run flow.

```bash
flutter pub get
flutter run -d windows            # or an Android device
flutter test                      # widget tests; see test/ for the live first-run flow
```
